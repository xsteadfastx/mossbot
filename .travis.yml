language: python
python:
        - 3.6
env:
        - TOXENV=py36
        - TOXENV=flake8
        - TOXENV=pylint
        - TOXENV=mypy
install:
        - travis_retry pip install tox
script: tox -e $TOXENV
